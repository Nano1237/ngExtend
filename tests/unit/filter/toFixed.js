describe('toFixed', function () {

    beforeEach(module('xt'));

    it('assigns a name', [
        'toFixed',
        function (toFixed) {
            expect(toFixed).toBeDefined();
        }
    ]);

});