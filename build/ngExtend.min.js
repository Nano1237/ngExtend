'use strict';
(function (d) {
    function a(e) {
        return function (b, c, a) {
            var f = d.isString(b) || d.isNumber(b);
            return a && !f ? (e.error("xt.toFixed: input is typeof " + typeof b), b) : (+b).toFixed(c || 0)
        }
    }

    a.$inject = ["$log"];
    d.module("xt", []).filter("toFixed", a).directive("xtSrcErr", function () {
        return {
            restrict: "A", scope: {xtSrcErr: "=xtSrcErr"}, link: function (a, b, c) {
                b.bind("error", function () {
                    c.src != a.xtSrcErr && c.$set("src", a.xtSrcErr)
                })
            }
        }
    })
})(angular);
